# Production n8n Configuration
# Updated: 2026-01-13
# n8n version: 2.3.4
# Task Runners: Internal mode (simplified for single-server SQLite deployment)
# Reverse Proxy: Traefik (external container managed separately)

services:
  n8n:
    image: docker.n8n.io/n8nio/n8n:${N8N_PROD_VERSION:-latest}
    container_name: n8n-production
    restart: always
    ports:
      - "5678:5678"
    
    # Traefik labels for reverse proxy routing
    labels:
      - traefik.enable=true
      - traefik.http.routers.n8n.rule=Host(`${SUBDOMAIN}.${DOMAIN_NAME}`)
      - traefik.http.routers.n8n.tls=true
      - traefik.http.routers.n8n.entrypoints=web,websecure
      - traefik.http.routers.n8n.tls.certresolver=mytlschallenge
      - traefik.http.middlewares.n8n.headers.SSLRedirect=true
      - traefik.http.middlewares.n8n.headers.STSSeconds=315360000
      - traefik.http.middlewares.n8n.headers.browserXSSFilter=true
      - traefik.http.middlewares.n8n.headers.contentTypeNosniff=true
      - traefik.http.middlewares.n8n.headers.forceSTSHeader=true
      - traefik.http.middlewares.n8n.headers.SSLHost=${DOMAIN_NAME}
      - traefik.http.middlewares.n8n.headers.STSIncludeSubdomains=true
      - traefik.http.middlewares.n8n.headers.STSPreload=true
      - traefik.http.routers.n8n.middlewares=n8n@docker
    
    environment:
      # Host Configuration
      - N8N_HOST=${SUBDOMAIN}.${DOMAIN_NAME}
      - N8N_PORT=5678
      - N8N_PROTOCOL=https
      - NODE_ENV=production
      - WEBHOOK_URL=https://${SUBDOMAIN}.${DOMAIN_NAME}/
      - GENERIC_TIMEZONE=${GENERIC_TIMEZONE}
      
      # Task Runners (Internal Mode for simplified deployment)
      - N8N_RUNNERS_ENABLED=true
      - N8N_RUNNERS_MODE=internal
      - N8N_RUNNERS_AUTH_TOKEN=${N8N_RUNNERS_AUTH_TOKEN}
      
      # Security (n8n v2.0+ hardening)
      - N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true
      - N8N_SKIP_AUTH_ON_OAUTH_CALLBACK=false
    
    volumes:
      - n8n_data:/home/node/.n8n
    
    networks:
      - default

networks:
  # Uses the external network where Traefik is running
  default:
    name: root_default
    external: true

volumes:
  # External volume for n8n data persistence
  n8n_data:
    external: true
